---js
const layout = "pages/posts-page.njk";

const title = 'Aktuality • MŠ Korálek';
const description = 'Nejnovější aktuality z MŠ Korálek v Libchavách. Přehled zpráv, oznámení a důležitých informací pro rodiče a děti.';
const permalink = '/spolecne/aktuality/ms-koralek/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}';
  
const category = 'spolecne';

const pagination = { 
    data: 'collections.aktuality',
    size: 30,
    reverse: true,
    alias: 'posts',
    before: function (paginationData, fullData) {
      return paginationData.filter((d) => d.data.section === "MŠ Korálek");
    }
  };
  
  const landing = {
    badge: 'Společné',
    heading: 'Aktuality',
    breadcrumbs: [
      { title: 'Domů', url: '/' },
      { title: 'Společné' },
      { title: 'Aktuality', url: '/spolecne/aktuality' },
      { title: 'MŠ Korálek' }
    ],
    image: {
      url: '/assets/images/services/skola/landing.jpg',
      alt: 'Jazyková učebna'
    }
  };

  const contentOne = {
    topper: 'Aktuality',
    heading: 'Novinky pro žáky a rodiče'
  };
---