---js
const layout = "pages/posts-page.njk";

const title = 'Aktuality • Jídelna';
const description = 'Nejnovější aktuality ze školní jídelny ZŠ a MŠ Libchavy. Přehled novinek, změn jídelníčků a důležitých oznámení pro rodiče.';
const permalink = '/spolecne/aktuality/jidelna/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}';
  
const category = 'spolecne';

const pagination = { 
    data: 'collections.aktuality',
    size: 30,
    reverse: true,
    alias: 'posts',
    before: function (paginationData, fullData) {
      return paginationData.filter((d) => d.data.section === "Jídelna");
    }
  };
  
  const landing = {
    badge: 'Společné',
    heading: 'Aktuality',
    breadcrumbs: [
      { title: 'Domů', url: '/' },
      { title: 'Společné' },
      { title: 'Aktuality', url: '/spolecne/aktuality' },
      { title: 'Jídelna' }
    ],
    image: {
      url: '/assets/images/services/skola/landing.jpg',
      alt: 'Jazyková učebna'
    }
  };

  const contentOne = {
    topper: 'Aktuality',
    heading: 'Novinky pro žáky a rodiče'
  };
---